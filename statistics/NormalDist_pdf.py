"""
from statistics import NormalDist
метод pdf описание и пример
https://alice.yandex.ru/chat/019bd5a7-abc9-4000-8b13-dfa27c9cbd46/

Метод pdf (Probability Density Function) используется для вычисления значения
функции плотности вероятности нормального распределения в конкретной точке.
Функция плотности вероятности показывает вероятность того,
что случайная величина примет определенное значение в нормальном распределении.

Практическое применение:
- Оценка вероятности появления конкретного значения в наборе данных
- Анализ распределения данных
- Статистическое моделирование
- Проверка гипотез

Важные замечания:
- Значение PDF не является вероятностью в классическом понимании
- PDF может быть больше 1
- Площадь под кривой PDF всегда равна 1
- Метод возвращает плотность вероятности только для конкретной точки, а не вероятность попадания в интервал
"""

from statistics import NormalDist

# Создаем нормальное распределение со средним 0 и стандартным отклонением 1
mmu=0
dist = NormalDist(mu=mmu, sigma=1)
# Вычисляем PDF для значения 1.5
a=1.5; b=2.0
print(dist)
print(f"PDF для x={mmu+a}: {dist.pdf(mmu+a)=}")  # Выведет значение плотности вероятности
print(f"PDF для x={mmu+b}: {dist.pdf(mmu+b)=}")  # Выведет значение плотности вероятности
print('----------')
# Пример с другим распределением
mmu=10
dist2 = NormalDist(mu=mmu, sigma=1)
print(dist2)
print(f"PDF для x={mmu+a}: {dist2.pdf(mmu+a)=}")  # Выведет значение плотности вероятности
print(f"PDF для x={mmu+b}: {dist2.pdf(mmu+b)=}")  # Выведет значение плотности вероятности
print('----------')
mmu=0
dist3 = NormalDist(mu=mmu, sigma=2)
print(dist3)

for i in range(-5, 6):
    print(f"PDF для x={mmu+i}: {dist3.pdf(mmu+i)=}")  # Выведет значение плотности вероятности

