"""
https://alice.yandex.ru/chat/019bc6aa-49e2-4000-bbf4-bd183fe898d1/
https://docs-python.ru/standart-library/modul-statistics-python/klass-normaldist-modulja-statistics/#ndist.inv_cdf

Метод ndist.inv_cdf() - вычисляет обратную кумулятивную функцию распределения,
также известную как квантильная функция или функция процентной точки.
Математически это записывается: x : P(X <= x) = p.
Находит значение x случайной величины X таким, что вероятность того,
что переменная будет меньше или равна этому значению, равна заданной вероятности p.

Основные области применения:
- Квантильный анализ - определение значений, соответствующих определенным процентилям распределения
- Построение доверительных интервалов - вычисление границ для заданного уровня доверия
- Статистическое моделирование - генерация значений с заданной вероятностью
- Проверка гипотез - определение критических значений
- Анализ рисков - оценка пороговых значений для различных уровней риска

Важные характеристики метода:
- Принимает на вход значение вероятности p из интервала (0, 1)
- Возвращает число с плавающей точкой
- Является обратной функцией к cdf()
- Может использоваться как для стандартного нормального распределения,
так и для любого нормального распределения с заданными параметрами
"""

# Импортируем необходимые модули
import matplotlib.pyplot as plt
import numpy as np
from statistics import NormalDist

# Создаем нормальное распределение со средним 0 и стандартным отклонением 1
normal_dist = NormalDist(mu=0, sigma=1)

# Создаем массив вероятностей от 0.01 до 0.99
probabilities = np.linspace(0.01, 0.99, 1000)

# Вычисляем значения x для каждой вероятности
x_values = [normal_dist.inv_cdf(p) for p in probabilities]
# inv_cdf(p) находит такое значение x, для которого вероятность P(X ≤ x) равна p.

# Строим график
plt.figure(figsize=(10, 6))
plt.plot(probabilities, x_values, label='Обратная CDF нормального распределения', color='green')

# Добавляем оформление графика
plt.title('Обратная кумулятивная функция распределения')
plt.xlabel('Вероятность (P)')
plt.ylabel('Значение (x)')
plt.grid(True)
plt.legend()
plt.axhline(0, color='black', linewidth=0.5)
plt.axvline(0.5, color='black', linewidth=0.5)
plt.grid(color='gray', linestyle='--', linewidth=0.5)
plt.tight_layout()

# Показываем график
plt.show()
# Важные особенности графика:
# При вероятности 0.5 значение x равно 0 (медиана распределения)
# При увеличении вероятности от 0.5 до 1 значения x растут
# При уменьшении вероятности от 0.5 до 0 значения x уменьшаются
# График симметричен относительно точки (0.5, 0)

# 1. Определение Z-оценок для различных процентилей:
# Z-оценка для 95-го процентиля
z_score_95 = NormalDist().inv_cdf(0.95)  # ≈ 1.6449

# 2. Расчет значений для пользовательского распределения:
# Распределение с mu=50, sigma=5
dist = NormalDist(mu=50, sigma=5)
value_80 = dist.inv_cdf(0.8)  # Значение для 80-го процентиля

# 3. Построение доверительного интервала
confidence = 0.95
upper_prob = (1 + confidence) / 2  # 0.975
lower_prob = (1 - confidence) / 2  # 0.025

z_upper = NormalDist().inv_cdf(upper_prob)  # ≈ 1.96
z_lower = NormalDist().inv_cdf(lower_prob)  # ≈ -1.96

# Важно помнить, что метод не принимает значения вероятности 0 и 1,
# так как они находятся за пределами допустимого диапазона.
# Для работы с крайними значениями следует использовать числа,
# максимально приближенные к 0 и 1.